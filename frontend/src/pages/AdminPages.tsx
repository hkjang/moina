import { Activity, Bot, Check, CircleGauge, FileCheck2, Flag, KeyRound, LockKeyhole, Plus, RefreshCw, Save, Search, Settings2, ShieldCheck, Trash2, Users, X } from 'lucide-react';
import { useEffect, useMemo, useState, type FormEvent } from 'react';
import { apiRequest, readableError } from '../api/client';
import { useAuth } from '../auth/AuthContext';
import { useToast } from '../components/ToastProvider';
import { Badge, Button, Card, EmptyState, ErrorState, Field, LoadingState, Modal, PageHeader, SectionHeader, SwitchField } from '../components/ui';
import { useApiQuery } from '../hooks/useApiQuery';
import { hasPermission } from '../navigation';
import { formatDate, listFrom } from '../utils/format';

function AdminTitle({ title, description, actions }: { title: string; description: string; actions?: React.ReactNode }) { return <PageHeader eyebrow="SERVICE ADMIN" title={title} description={description} actions={actions}/>; }
function Table({ caption, headers, children }: { caption: string; headers: string[]; children: React.ReactNode }) { return <div className="table-scroll custom-scrollbar"><table className="data-table"><caption className="sr-only">{caption}</caption><thead><tr>{headers.map((header) => <th scope="col" key={header}>{header}</th>)}</tr></thead><tbody>{children}</tbody></table></div>; }
const statusTone = (value: unknown) => ['active', 'approved', 'resolved', 'success', '활성', '정상'].includes(String(value).toLowerCase()) ? 'positive' as const : ['pending', 'review', '대기'].some((item) => String(value).toLowerCase().includes(item)) ? 'warning' as const : ['disabled', 'rejected', 'blocked', 'failed', '비활성'].some((item) => String(value).toLowerCase().includes(item)) ? 'danger' as const : 'neutral' as const;

export function AdminOverviewPage() {
  const query = useApiQuery<Record<string, unknown>>('/admin/stats');
  const data = query.data || {};
  const metrics = [
    { label: '전체 사용자', value: Number(data.userCount || data.users || 0), icon: Users, tone: 'brand' },
    { label: '오늘의 모인', value: Number(data.todayPostCount || data.todayPosts || 0), icon: Activity, tone: 'positive' },
    { label: '처리 대기 신고', value: Number(data.pendingReportCount || data.pendingReports || 0), icon: Flag, tone: 'warning' },
    { label: '승인 대기', value: Number(data.pendingApprovalCount || data.pendingApprovals || 0), icon: FileCheck2, tone: 'neutral' },
  ];
  return <div className="page-stack"><AdminTitle title="관리 대시보드" description="개인 활동과 분리된 서비스 운영·보안 현황을 확인합니다." actions={<Button onClick={query.reload}><RefreshCw/>새로고침</Button>}/>{query.loading ? <LoadingState/> : query.error ? <ErrorState message={query.error} onRetry={query.reload}/> : <><div className="metric-grid">{metrics.map((metric) => <Card className="metric-card" key={metric.label}><span className={`metric-icon ${metric.tone}`}><metric.icon/></span><span><small>{metric.label}</small><strong>{metric.value.toLocaleString('ko-KR')}</strong></span></Card>)}</div><div className="admin-dashboard-grid"><Card><SectionHeader title="서비스 상태"/><div className="health-list"><p><span>API</span><Badge tone="positive">정상</Badge></p><p><span>PostgreSQL</span><Badge tone={statusTone(data.databaseStatus || 'active')}>{String(data.databaseStatus || '연결됨')}</Badge></p><p><span>실시간 알림</span><Badge tone={statusTone(data.websocketStatus || 'active')}>{String(data.websocketStatus || '운영 중')}</Badge></p><p><span>AI 공급자</span><Badge tone={data.aiEnabled ? 'positive' : 'neutral'}>{data.aiEnabled ? '사용 중' : '꺼짐'}</Badge></p></div></Card><Card><SectionHeader title="운영 안내"/><div className="admin-callout"><ShieldCheck/><span><strong>관리 활동은 모두 감사 로그에 남습니다.</strong><p>콘텐츠 삭제, 사용자 제재, 역할·설정 변경은 최소 권한으로 수행하세요.</p></span></div></Card></div></>}</div>;
}

interface AdminUser { id: string; username: string; displayName?: string; email?: string; provider?: string; roles?: string[]; active?: boolean; createdAt?: string }
interface RoleRow { name: string; permissions: string[]; description?: string }
function roleRows(value: unknown): RoleRow[] { if (Array.isArray(value)) return value as RoleRow[]; const raw = value as { items?: RoleRow[]; roles?: RoleRow[] } | undefined; return raw?.items || raw?.roles || []; }

export function AdminUsersPage() {
  const { notify } = useToast(); const query = useApiQuery<unknown>('/admin/users?limit=100'); const rolesQuery = useApiQuery<unknown>('/admin/roles');
  const [search, setSearch] = useState(''); const [createOpen, setCreateOpen] = useState(false); const [working, setWorking] = useState<string | null>(null);
  const [draft, setDraft] = useState({ username: '', displayName: '', email: '', password: '', role: 'member' });
  const users = listFrom<AdminUser>(query.data as AdminUser[] | { items?: AdminUser[] } | undefined).filter((user) => `${user.username} ${user.displayName} ${user.email}`.toLowerCase().includes(search.toLowerCase()));
  const roles = roleRows(rolesQuery.data);
  useEffect(() => { if (roles.length && !roles.some((role) => role.name === draft.role)) setDraft((current) => ({ ...current, role: roles[0].name })); }, [roles]);
  const create = async (event: FormEvent) => { event.preventDefault(); setWorking('create'); try { await apiRequest('/admin/users', { method: 'POST', body: { ...draft, roles: [draft.role] } }); notify('로컬 사용자를 추가했습니다.', 'success'); setCreateOpen(false); query.reload(); } catch (error) { notify(readableError(error), 'error'); } finally { setWorking(null); } };
  const toggle = async (user: AdminUser) => { setWorking(user.id); try { await apiRequest(`/admin/users/${encodeURIComponent(user.id)}`, { method: 'PATCH', body: { active: user.active === false } }); notify(user.active === false ? '사용자를 활성화했습니다.' : '사용자를 비활성화했습니다.', 'success'); query.reload(); } catch (error) { notify(readableError(error), 'error'); } finally { setWorking(null); } };
  const updateRole = async (user: AdminUser, role: string) => { setWorking(user.id); try { await apiRequest(`/admin/users/${encodeURIComponent(user.id)}`, { method: 'PATCH', body: { roles: [role] } }); notify('사용자 역할을 변경했습니다.', 'success'); query.reload(); } catch (error) { notify(readableError(error), 'error'); } finally { setWorking(null); } };
  return <div className="page-stack"><AdminTitle title="사용자 관리" description="로컬·OIDC 사용자 상태와 역할을 관리합니다." actions={<Button variant="primary" onClick={() => setCreateOpen(true)}><Plus/>사용자 추가</Button>}/><Card><div className="table-toolbar"><label className="table-search"><Search/><input value={search} onChange={(event) => setSearch(event.target.value)} aria-label="사용자 검색" placeholder="이름, 사용자 이름, 이메일 검색"/></label><Button onClick={query.reload}><RefreshCw/>새로고침</Button></div>{query.loading || rolesQuery.loading ? <LoadingState/> : query.error || rolesQuery.error ? <ErrorState message={query.error || rolesQuery.error || '사용자 정보를 불러오지 못했습니다.'} onRetry={() => { query.reload(); rolesQuery.reload(); }}/> : users.length ? <Table caption="사용자 목록" headers={['사용자', '인증', '역할', '상태', '등록일', '관리']}>{users.map((user) => <tr key={user.id}><td><strong>{user.displayName || user.username}</strong><small>{user.email || `@${user.username}`}</small></td><td>{user.provider === 'oidc' ? 'Keycloak OIDC' : '로컬'}</td><td><select aria-label={`${user.username} 역할`} value={user.roles?.[0] || ''} onChange={(event) => void updateRole(user, event.target.value)} disabled={working === user.id}>{roles.map((role) => <option key={role.name} value={role.name}>{role.name}</option>)}</select></td><td><Badge tone={user.active === false ? 'danger' : 'positive'}>{user.active === false ? '비활성' : '활성'}</Badge></td><td>{formatDate(user.createdAt, false)}</td><td><Button size="small" variant="ghost" onClick={() => void toggle(user)} disabled={working === user.id}>{user.active === false ? '활성화' : '비활성화'}</Button></td></tr>)}</Table> : <EmptyState title="사용자가 없습니다" description="첫 사용자를 추가하거나 OIDC 자동 등록을 설정하세요."/>}</Card><Modal open={createOpen} onOpenChange={setCreateOpen} title="로컬 사용자 추가" description="임시 비밀번호는 안전한 채널로 전달하세요."><form className="settings-form" onSubmit={create}><div className="form-grid"><Field label="사용자 이름"><input required value={draft.username} onChange={(event) => setDraft({ ...draft, username: event.target.value })}/></Field><Field label="표시 이름"><input required value={draft.displayName} onChange={(event) => setDraft({ ...draft, displayName: event.target.value })}/></Field><Field label="이메일"><input type="email" value={draft.email} onChange={(event) => setDraft({ ...draft, email: event.target.value })}/></Field><Field label="초기 비밀번호" help="12자 이상"><input type="password" required minLength={12} value={draft.password} onChange={(event) => setDraft({ ...draft, password: event.target.value })}/></Field><Field label="역할"><select value={draft.role} onChange={(event) => setDraft({ ...draft, role: event.target.value })}>{roles.map((role) => <option key={role.name}>{role.name}</option>)}</select></Field></div><div className="form-actions"><Button type="button" onClick={() => setCreateOpen(false)}>취소</Button><Button type="submit" variant="primary" disabled={working === 'create'}>사용자 추가</Button></div></form></Modal></div>;
}

interface AdminPost { id: string; content?: string; authorUsername?: string; status?: string; reportCount?: number; createdAt?: string }
export function AdminContentPage() {
  const { notify } = useToast(); const query = useApiQuery<unknown>('/admin/posts?limit=100'); const [search, setSearch] = useState(''); const [working, setWorking] = useState<string | null>(null);
  const posts = listFrom<AdminPost>(query.data as AdminPost[] | { items?: AdminPost[] } | undefined).filter((post) => `${post.content} ${post.authorUsername}`.toLowerCase().includes(search.toLowerCase()));
  const remove = async (post: AdminPost) => { if (!window.confirm('이 모인을 운영 정책에 따라 삭제할까요?')) return; setWorking(post.id); try { await apiRequest(`/admin/posts/${encodeURIComponent(post.id)}`, { method: 'PATCH', body: { status: 'deleted' } }); notify('모인을 삭제 상태로 변경하고 감사 로그에 기록했습니다.', 'success'); query.reload(); } catch (error) { notify(readableError(error), 'error'); } finally { setWorking(null); } };
  return <div className="page-stack"><AdminTitle title="콘텐츠 관리" description="모인과 커뮤니티 콘텐츠를 검색하고 운영 정책에 따라 조치합니다."/><Card><div className="table-toolbar"><label className="table-search"><Search/><input value={search} onChange={(event) => setSearch(event.target.value)} aria-label="콘텐츠 검색" placeholder="내용 또는 작성자 검색"/></label><Button onClick={query.reload}><RefreshCw/>새로고침</Button></div>{query.loading ? <LoadingState/> : query.error ? <ErrorState message={query.error} onRetry={query.reload}/> : posts.length ? <Table caption="모인 관리 목록" headers={['내용', '작성자', '신고', '상태', '작성일', '조치']}>{posts.map((post) => <tr key={post.id}><td><strong className="table-content">{post.content || '내용 없음'}</strong><small>{post.id}</small></td><td>@{post.authorUsername || '알 수 없음'}</td><td>{post.reportCount || 0}건</td><td><Badge tone={statusTone(post.status || 'active')}>{post.status || '게시 중'}</Badge></td><td>{formatDate(post.createdAt)}</td><td><Button size="small" variant="ghost" className="text-danger" onClick={() => void remove(post)} disabled={working === post.id}><Trash2/>삭제</Button></td></tr>)}</Table> : <EmptyState title="관리할 콘텐츠가 없습니다" description="새 모인이 게시되면 여기에 표시됩니다."/>}</Card></div>;
}

interface ReportRow { id: string; targetType?: string; targetId?: string; reason?: string; reporterUsername?: string; status?: string; createdAt?: string }
export function AdminReportsPage() {
  const { notify } = useToast(); const query = useApiQuery<unknown>('/admin/reports?limit=100'); const reports = listFrom<ReportRow>(query.data as ReportRow[] | { items?: ReportRow[] } | undefined); const [decision, setDecision] = useState<{ report: ReportRow; action: 'resolve' | 'reject'; note: string } | null>(null); const [saving, setSaving] = useState(false);
  const submit = async (event: FormEvent) => { event.preventDefault(); if (!decision) return; setSaving(true); try { await apiRequest(`/admin/reports/${encodeURIComponent(decision.report.id)}`, { method: 'PATCH', body: { status: decision.action === 'resolve' ? 'resolved' : 'dismissed', resolution: decision.note } }); notify(decision.action === 'resolve' ? '신고를 처리했습니다.' : '신고를 기각했습니다.', 'success'); setDecision(null); query.reload(); } catch (error) { notify(readableError(error), 'error'); } finally { setSaving(false); } };
  return <div className="page-stack"><AdminTitle title="신고·제재" description="신고 내용을 검토하고 일관된 운영 정책에 따라 처리합니다." actions={<Button onClick={query.reload}><RefreshCw/>새로고침</Button>}/><Card>{query.loading ? <LoadingState/> : query.error ? <ErrorState message={query.error} onRetry={query.reload}/> : reports.length ? <Table caption="신고 목록" headers={['대상', '신고 사유', '신고자', '상태', '접수일', '검토']}>{reports.map((report) => <tr key={report.id}><td><strong>{report.targetType || '대상'} · {report.targetId}</strong><small>{report.id}</small></td><td>{report.reason || '사유 없음'}</td><td>@{report.reporterUsername || '알 수 없음'}</td><td><Badge tone={statusTone(report.status || 'pending')}>{report.status || '검토 대기'}</Badge></td><td>{formatDate(report.createdAt)}</td><td><div className="row-actions"><Button size="small" onClick={() => setDecision({ report, action: 'reject', note: '' })}>기각</Button><Button size="small" variant="danger" onClick={() => setDecision({ report, action: 'resolve', note: '' })}>제재 처리</Button></div></td></tr>)}</Table> : <EmptyState title="대기 중인 신고가 없습니다" description="새 신고가 접수되면 이곳에서 검토할 수 있습니다."/>}</Card><Modal open={Boolean(decision)} onOpenChange={(open) => !open && setDecision(null)} title={decision?.action === 'resolve' ? '신고 제재 처리' : '신고 기각'} description="결정과 메모는 감사 로그에 기록됩니다.">{decision && <form className="settings-form" onSubmit={submit}><Field label="검토 메모"><textarea required rows={4} value={decision.note} onChange={(event) => setDecision({ ...decision, note: event.target.value })}/></Field><div className="form-actions"><Button type="button" onClick={() => setDecision(null)}>취소</Button><Button type="submit" variant={decision.action === 'resolve' ? 'danger' : 'primary'} disabled={saving}>{saving ? '처리 중…' : '결정 확정'}</Button></div></form>}</Modal></div>;
}

interface ApprovalRow { id: string; action?: string; targetType?: string; targetId?: string; requesterUsername?: string; status?: string; summary?: string; createdAt?: string }
export function AdminApprovalsPage() {
  const { notify } = useToast(); const status = useApiQuery<{ enabled?: boolean; approvalEnabled?: boolean; pending?: number }>('/workflow/status'); const query = useApiQuery<unknown>('/approvals?limit=100'); const approvals = listFrom<ApprovalRow>(query.data as ApprovalRow[] | { items?: ApprovalRow[] } | undefined); const [decision, setDecision] = useState<{ item: ApprovalRow; type: 'approve' | 'reject'; note: string } | null>(null); const [saving, setSaving] = useState(false); const enabled = status.data?.enabled ?? status.data?.approvalEnabled;
  const submit = async (event: FormEvent) => { event.preventDefault(); if (!decision) return; setSaving(true); try { await apiRequest(`/approvals/${encodeURIComponent(decision.item.id)}/${decision.type}`, { method: 'POST', body: { comment: decision.note } }); notify(decision.type === 'approve' ? '요청을 승인했습니다.' : '요청을 반려했습니다.', 'success'); setDecision(null); query.reload(); status.reload(); } catch (error) { notify(readableError(error), 'error'); } finally { setSaving(false); } };
  return <div className="page-stack"><AdminTitle title="검토·승인" description="관리자가 활성화한 팀장 검토 프로세스의 요청을 승인하거나 반려합니다."/>{status.loading ? <LoadingState/> : status.error ? <ErrorState message={status.error} onRetry={status.reload}/> : !enabled && approvals.length === 0 ? <Card><EmptyState title="승인 프로세스가 꺼져 있습니다" description="현재 새 요청에는 검토·승인·반려 단계가 적용되지 않습니다. 일반 설정에서 필요할 때 켤 수 있습니다."/></Card> : <Card><SectionHeader title="승인 대기 요청" description={!enabled ? '프로세스를 끄기 전에 생성된 요청만 표시합니다.' : '승인만으로 외부 작업이 자동 실행되지는 않습니다.'} action={<Button onClick={query.reload}><RefreshCw/>새로고침</Button>}/>{query.loading ? <LoadingState/> : query.error ? <ErrorState message={query.error} onRetry={query.reload}/> : approvals.length ? <Table caption="승인 요청 목록" headers={['요청', '대상', '요청자', '요약', '상태', '요청일', '검토']}>{approvals.map((item) => <tr key={item.id}><td><strong>{item.action || '설정 변경'}</strong><small>{item.id}</small></td><td>{item.targetType} · {item.targetId}</td><td>@{item.requesterUsername || '시스템'}</td><td>{item.summary || '상세 내용을 확인하세요.'}</td><td><Badge tone="warning">{item.status || '승인 대기'}</Badge></td><td>{formatDate(item.createdAt)}</td><td><div className="row-actions"><Button size="small" onClick={() => setDecision({ item, type: 'reject', note: '' })}><X/>반려</Button><Button size="small" variant="primary" onClick={() => setDecision({ item, type: 'approve', note: '' })}><Check/>승인</Button></div></td></tr>)}</Table> : <EmptyState title="처리할 승인 요청이 없습니다" description="정책에 맞는 요청이 생성되면 여기에 표시됩니다."/>}</Card>}<Modal open={Boolean(decision)} onOpenChange={(open) => !open && setDecision(null)} title={decision?.type === 'approve' ? '요청 승인' : '요청 반려'} description={decision?.item.summary || '결정 전 요청 내용을 다시 확인하세요.'}>{decision && <form className="settings-form" onSubmit={submit}><Field label={decision.type === 'reject' ? '반려 사유' : '검토 의견'}><textarea required={decision.type === 'reject'} rows={4} value={decision.note} onChange={(event) => setDecision({ ...decision, note: event.target.value })}/></Field><div className="form-actions"><Button type="button" onClick={() => setDecision(null)}>취소</Button><Button type="submit" variant={decision.type === 'approve' ? 'primary' : 'danger'} disabled={saving || (decision.type === 'reject' && !decision.note.trim())}>결정 확정</Button></div></form>}</Modal></div>;
}

const permissionCatalog = [
  ['admin:access', '서비스 관리자 진입'], ['users:manage', '사용자 관리'], ['posts:manage', '모인 관리'], ['moderation:manage', '신고·제재 관리'], ['approvals:review', '검토·승인'], ['roles:manage', '역할·권한 관리'], ['settings:manage', '서비스 설정'], ['audit:read', '감사 로그 조회'], ['posts:read', '모인 조회'], ['posts:write', '모인 작성'], ['social:write', 'Link·Signal 활동'], ['ai:use', 'AI 사용'], ['mcp:use', 'MCP 사용'], ['keys:manage', '개인 키 관리'],
] as const;
export function AdminRolesPage() {
  const { user } = useAuth(); const { notify } = useToast(); const query = useApiQuery<unknown>('/admin/roles'); const roles = roleRows(query.data); const [selected, setSelected] = useState(''); const [draft, setDraft] = useState<string[]>([]); const [saving, setSaving] = useState(false); const role = roles.find((item) => item.name === selected) || roles[0]; const locked = role?.name === 'super_admin' || !hasPermission(user?.permissions, 'roles:manage');
  useEffect(() => { if (roles.length && !selected) setSelected(roles[0].name); }, [roles, selected]); useEffect(() => { if (role) setDraft(role.permissions || []); }, [role?.name]);
  const save = async () => { if (!role) return; setSaving(true); try { await apiRequest('/admin/roles', { method: 'PUT', body: { roles: roles.map((item) => item.name === role.name ? { ...item, permissions: draft } : item) } }); notify('역할 권한 정책을 저장했습니다.', 'success'); query.reload(); } catch (error) { notify(readableError(error), 'error'); } finally { setSaving(false); } };
  return <div className="page-stack"><AdminTitle title="역할·권한" description="역할별 기능 권한을 변경하고 개인 키의 최대 권한 범위를 제어합니다."/>{query.loading ? <LoadingState/> : query.error ? <ErrorState message={query.error} onRetry={query.reload}/> : roles.length ? <div className="role-layout"><Card className="role-sidebar"><SectionHeader title="역할"/>{roles.map((item) => <button type="button" className={role?.name === item.name ? 'active' : ''} onClick={() => setSelected(item.name)} key={item.name}><span><strong>{item.name}</strong><small>{item.permissions?.length || 0}개 권한</small></span>{item.name === 'super_admin' && <Badge>보호</Badge>}</button>)}</Card><Card><SectionHeader title={role?.name || '역할'} description={locked ? '최고 관리자 잠금을 방지하기 위해 이 역할은 읽기 전용입니다.' : '허용할 기능을 선택하세요.'} action={<Button variant="primary" onClick={() => void save()} disabled={locked || saving}><Save/>{saving ? '저장 중…' : '권한 저장'}</Button>}/><div className="permission-grid">{permissionCatalog.map(([permission, label]) => <label key={permission}><input type="checkbox" checked={draft.includes(permission) || draft.includes('*')} disabled={locked || draft.includes('*')} onChange={(event) => setDraft(event.target.checked ? [...draft, permission] : draft.filter((item) => item !== permission))}/><span><strong>{label}</strong><code>{permission}</code></span></label>)}</div></Card></div> : <EmptyState title="역할이 없습니다" description="서비스에는 최소 하나의 super_admin 역할이 필요합니다."/>}</div>;
}

interface OIDCSettings { enabled?: boolean; issuerUrl?: string; clientId?: string; clientSecret?: string; clientSecretConfigured?: boolean; clearClientSecret?: boolean; redirectUrl?: string; scopes?: string[]; autoProvision?: boolean; defaultRoles?: string[]; roleClaim?: string; roleMappings?: Record<string, string[]>; allowInsecureHttp?: boolean }
export function AdminOIDCPage() {
  const { notify } = useToast();
  const query = useApiQuery<OIDCSettings>('/admin/oidc');
  const rolesQuery = useApiQuery<unknown>('/admin/roles');
  const [form, setForm] = useState<OIDCSettings>({ enabled: false, scopes: ['openid', 'profile', 'email'], autoProvision: true, defaultRoles: [], roleClaim: 'realm_access.roles', roleMappings: {} });
  const [mappingsText, setMappingsText] = useState('{}');
  const [working, setWorking] = useState<'save' | 'test' | null>(null);
  const roles = roleRows(rolesQuery.data);
  useEffect(() => { if (query.data) { setForm({ ...query.data, clientSecret: '', scopes: query.data.scopes?.length ? query.data.scopes : ['openid', 'profile', 'email'] }); setMappingsText(JSON.stringify(query.data.roleMappings || {}, null, 2)); } }, [query.data]);
  const save = async (test = false) => {
    let roleMappings: Record<string, string[]>;
    try { const parsed = JSON.parse(mappingsText) as unknown; if (!parsed || Array.isArray(parsed) || typeof parsed !== 'object' || Object.values(parsed as Record<string, unknown>).some((value) => !Array.isArray(value) || value.some((item) => typeof item !== 'string'))) throw new Error(); roleMappings = parsed as Record<string, string[]>; }
    catch { notify('역할 매핑은 {"Keycloak역할":["MOINA역할"]} 형식의 JSON이어야 합니다.', 'error'); return; }
    setWorking(test ? 'test' : 'save');
    try { await apiRequest('/admin/oidc', { method: 'PUT', body: { ...form, roleMappings } }); if (test) await apiRequest('/admin/oidc/test', { method: 'POST' }); notify(test ? 'OIDC 설정을 저장하고 연결을 확인했습니다.' : 'OIDC 설정을 저장했습니다.', 'success'); query.reload(); }
    catch (error) { notify(readableError(error), 'error'); }
    finally { setWorking(null); }
  };
  return <div className="page-stack"><AdminTitle title="Keycloak OIDC" description="Issuer URL, Client ID와 Client Secret만으로 SSO를 자동 연결합니다."/>{query.loading || rolesQuery.loading ? <LoadingState/> : query.error || rolesQuery.error ? <ErrorState message={query.error || rolesQuery.error || 'OIDC 설정을 불러오지 못했습니다.'} onRetry={() => { query.reload(); rolesQuery.reload(); }}/> : <Card><SectionHeader title="OIDC 연결" description="Issuer Discovery 문서에서 인증·토큰 엔드포인트를 자동 구성합니다." action={<SwitchField label="Keycloak 로그인 사용" description="로그인 화면에 SSO 버튼을 표시합니다." checked={form.enabled === true} onChange={(enabled) => setForm({ ...form, enabled })}/>}/><div className="form-grid"><Field label="Issuer URL"><input type="url" required={form.enabled} value={form.issuerUrl || ''} onChange={(event) => setForm({ ...form, issuerUrl: event.target.value })} placeholder="https://keycloak.internal/realms/moina"/></Field><Field label="Client ID"><input required={form.enabled} value={form.clientId || ''} onChange={(event) => setForm({ ...form, clientId: event.target.value })} placeholder="moina"/></Field><Field label="Client Secret" help={form.clientSecretConfigured ? '비워두면 저장된 Secret을 유지합니다.' : '공개 클라이언트라면 비울 수 있습니다.'}><input type="password" value={form.clientSecret || ''} disabled={form.clearClientSecret} onChange={(event) => setForm({ ...form, clientSecret: event.target.value, clearClientSecret: false })}/></Field><Field label="Redirect URL" help="비워두면 현재 접속 주소를 기준으로 자동 구성합니다."><input type="url" value={form.redirectUrl || ''} onChange={(event) => setForm({ ...form, redirectUrl: event.target.value })} placeholder={`${window.location.origin}/api/v1/auth/oidc/callback`}/></Field><Field label="Scopes"><input value={(form.scopes || []).join(' ')} onChange={(event) => setForm({ ...form, scopes: event.target.value.split(/\s+/).filter(Boolean) })}/></Field><Field label="역할 Claim 경로"><input value={form.roleClaim || ''} onChange={(event) => setForm({ ...form, roleClaim: event.target.value })} placeholder="realm_access.roles"/></Field><Field label="기본 역할"><select value={form.defaultRoles?.[0] || ''} onChange={(event) => setForm({ ...form, defaultRoles: [event.target.value] })}><option value="">역할 선택</option>{roles.map((role) => <option key={role.name}>{role.name}</option>)}</select></Field></div><Field label="역할 매핑 JSON" help={'예: {"keycloak-admin":["admin"]}'}><textarea rows={6} spellCheck={false} value={mappingsText} onChange={(event) => setMappingsText(event.target.value)}/></Field>{form.clientSecretConfigured && <SwitchField label="저장된 Client Secret 삭제" description="저장하면 기존 Secret 암호문을 제거합니다." checked={form.clearClientSecret === true} onChange={(checked) => setForm({ ...form, clearClientSecret: checked, clientSecret: checked ? '' : form.clientSecret })}/>}<SwitchField label="첫 로그인 사용자 자동 등록" description="OIDC 프로필로 사용자를 만들고 기본 역할을 부여합니다." checked={form.autoProvision !== false} onChange={(checked) => setForm({ ...form, autoProvision: checked })}/><SwitchField label="폐쇄망 HTTP 허용" description="TLS가 없는 신뢰된 내부망에서만 명시적으로 사용하세요." checked={form.allowInsecureHttp === true} onChange={(checked) => setForm({ ...form, allowInsecureHttp: checked })}/><div className="callback-box"><LockKeyhole/><span><strong>Keycloak Valid redirect URI</strong><code>{form.redirectUrl || `${window.location.origin}/api/v1/auth/oidc/callback`}</code></span></div><div className="form-actions"><Button onClick={() => void save(true)} disabled={Boolean(working) || !form.issuerUrl || !form.clientId}>{working === 'test' ? '연결 확인 중…' : '저장 후 연결 테스트'}</Button><Button variant="primary" onClick={() => void save(false)} disabled={Boolean(working) || (form.enabled && (!form.issuerUrl || !form.clientId))}>{working === 'save' ? '저장 중…' : 'OIDC 설정 저장'}</Button></div></Card>}</div>;
}

interface AISettings { enabled?: boolean; baseUrl?: string; apiKey?: string; apiKeyConfigured?: boolean; clearApiKey?: boolean; model?: string; apiStyle?: 'responses' | 'chat_completions'; defaultMaxTokens?: number; maxTokens?: number; timeoutSeconds?: number; allowInsecureHttp?: boolean }
export function AdminAIPage() {
  const { notify } = useToast(); const query = useApiQuery<AISettings>('/admin/ai'); const [form, setForm] = useState<AISettings>({ enabled: false, apiStyle: 'responses', defaultMaxTokens: 4096, maxTokens: 262144, timeoutSeconds: 300 }); const [working, setWorking] = useState<'save' | 'test' | null>(null);
  useEffect(() => { if (query.data) setForm({ ...query.data, apiKey: '' }); }, [query.data]);
  const save = async (test = false) => { setWorking(test ? 'test' : 'save'); try { await apiRequest('/admin/ai', { method: 'PUT', body: form }); if (test) await apiRequest('/admin/ai/test', { method: 'POST' }); notify(test ? 'AI 설정을 저장하고 스트리밍 연결을 확인했습니다.' : 'AI 설정을 저장했습니다.', 'success'); query.reload(); } catch (error) { notify(readableError(error), 'error'); } finally { setWorking(null); } };
  return <div className="page-stack"><AdminTitle title="AI 설정" description="OpenAI 호환 공급자와 최대 256K 토큰 스트리밍 정책을 관리합니다."/>{query.loading ? <LoadingState/> : query.error ? <ErrorState message={query.error} onRetry={query.reload}/> : <Card><SectionHeader title="AI 공급자" description="자격 증명은 ENCRYPTION_KEY로 암호화되고 화면에 다시 노출되지 않습니다." action={<SwitchField label="AI 기능 사용" description="ai:use 권한 사용자에게 메뉴를 표시합니다." checked={form.enabled === true} onChange={(enabled) => setForm({ ...form, enabled })}/>}/>{form.enabled && <><div className="form-grid"><Field label="API 기본 주소" help="오프라인망의 OpenAI 호환 엔드포인트를 입력하세요."><input type="url" required value={form.baseUrl || ''} onChange={(event) => setForm({ ...form, baseUrl: event.target.value })} placeholder="https://ai.internal/v1"/></Field><Field label="모델 이름"><input required value={form.model || ''} onChange={(event) => setForm({ ...form, model: event.target.value })}/></Field><Field label="API Key" help={form.apiKeyConfigured ? '비워두면 저장된 키를 유지합니다.' : '공급자가 요구하는 경우 입력하세요.'}><input type="password" value={form.apiKey || ''} disabled={form.clearApiKey} onChange={(event) => setForm({ ...form, apiKey: event.target.value, clearApiKey: false })}/></Field><Field label="API 방식"><select value={form.apiStyle} onChange={(event) => setForm({ ...form, apiStyle: event.target.value as AISettings['apiStyle'] })}><option value="responses">Responses API</option><option value="chat_completions">Chat Completions API</option></select></Field><Field label="사용자 기본 최대 토큰"><input type="number" min="1" max="262144" value={form.defaultMaxTokens || 4096} onChange={(event) => setForm({ ...form, defaultMaxTokens: Math.min(262144, Math.max(1, Number(event.target.value))) })}/></Field><Field label="서비스 최대 토큰" help="최대 262,144(256K)"><input type="number" min="1" max="262144" value={form.maxTokens || 262144} onChange={(event) => setForm({ ...form, maxTokens: Math.min(262144, Math.max(1, Number(event.target.value))) })}/></Field><Field label="요청 제한 시간(초)"><input type="number" min="10" max="3600" value={form.timeoutSeconds || 300} onChange={(event) => setForm({ ...form, timeoutSeconds: Number(event.target.value) })}/></Field></div>{form.apiKeyConfigured && <SwitchField label="저장된 API Key 삭제" description="저장하면 기존 API Key 암호문을 제거합니다." checked={form.clearApiKey === true} onChange={(checked) => setForm({ ...form, clearApiKey: checked, apiKey: checked ? '' : form.apiKey })}/>}<SwitchField label="폐쇄망 HTTP 허용" description="TLS가 없는 신뢰된 내부 AI 엔드포인트에서만 사용하세요." checked={form.allowInsecureHttp === true} onChange={(checked) => setForm({ ...form, allowInsecureHttp: checked })}/><div className="streaming-note"><Bot/><span><strong>응답은 항상 SSE로 스트리밍됩니다.</strong><p>사용자는 관리자가 정한 서비스 상한 안에서 요청별 토큰 수를 선택할 수 있습니다.</p></span></div></>}<div className="form-actions"><Button onClick={() => void save(true)} disabled={Boolean(working) || (form.enabled && (!form.baseUrl || !form.model))}>{working === 'test' ? '연결 확인 중…' : '저장 후 연결 테스트'}</Button><Button variant="primary" onClick={() => void save(false)} disabled={Boolean(working) || (form.enabled && (!form.baseUrl || !form.model))}>{working === 'save' ? '저장 중…' : 'AI 설정 저장'}</Button></div></Card>}</div>;
}

interface SettingRow { key: string; value?: unknown; sensitive?: boolean; configured?: boolean; revision?: number }
interface WorkflowSettings { enabled?: boolean; actions?: string[]; approverRoles?: string[] }
export function AdminSettingsPage() {
  const { notify } = useToast(); const settings = useApiQuery<{ items?: SettingRow[] } | SettingRow[]>('/admin/settings'); const workflowQuery = useApiQuery<WorkflowSettings>('/admin/workflow'); const rolesQuery = useApiQuery<unknown>('/admin/roles'); const [working, setWorking] = useState<string | null>(null);
  const rows = Array.isArray(settings.data) ? settings.data : settings.data?.items || [];
  const findValue = <T,>(key: string, fallback: T): T => { const value = rows.find((item) => item.key === key)?.value; return value && typeof value === 'object' ? { ...fallback, ...value as T } : fallback; };
  const [general, setGeneral] = useState({ serviceName: 'moina', sessionMinutes: 720, defaultTimezone: 'Asia/Seoul', allowRegistration: false }); const [api, setAPI] = useState({ enabled: true, mcpEnabled: true, rateLimitPerMinute: 120 }); const [media, setMedia] = useState({ maxUploadBytes: 10 * 1024 * 1024, maxPerPost: 4 }); const [workflow, setWorkflow] = useState<WorkflowSettings>({ enabled: false, actions: ['post.publish'], approverRoles: [] });
  useEffect(() => { if (settings.data) { setGeneral(findValue('service.general', general)); setAPI(findValue('api.access', api)); setMedia(findValue('media.config', media)); } }, [settings.data]); useEffect(() => { if (workflowQuery.data) setWorkflow(workflowQuery.data); }, [workflowQuery.data]);
  const saveSetting = async (key: string, value: unknown) => { setWorking(key); const row = rows.find((item) => item.key === key); try { await apiRequest(`/admin/settings/${encodeURIComponent(key)}`, { method: 'PUT', body: { value, sensitive: false, revision: row?.revision } }); notify('서비스 설정을 저장했습니다.', 'success'); settings.reload(); } catch (error) { notify(readableError(error), 'error'); } finally { setWorking(null); } };
  const saveWorkflow = async () => { setWorking('workflow'); try { await apiRequest('/admin/workflow', { method: 'PUT', body: workflow }); notify('검토·승인 정책을 저장했습니다.', 'success'); workflowQuery.reload(); } catch (error) { notify(readableError(error), 'error'); } finally { setWorking(null); } };
  const roleNames = roleRows(rolesQuery.data).filter((role) => role.permissions?.includes('approvals:review') || role.permissions?.includes('*')).map((role) => role.name);
  return <div className="page-stack"><AdminTitle title="일반 설정" description="서비스 공통 정책과 API·MCP, 미디어, 선택형 검토·승인 프로세스를 관리합니다."/>{settings.loading || workflowQuery.loading || rolesQuery.loading ? <LoadingState/> : settings.error || workflowQuery.error || rolesQuery.error ? <ErrorState message={settings.error || workflowQuery.error || rolesQuery.error || '설정을 불러오지 못했습니다.'} onRetry={() => { settings.reload(); workflowQuery.reload(); rolesQuery.reload(); }}/> : <div className="settings-layout"><Card><SectionHeader title="서비스 기본" action={<Button variant="primary" onClick={() => void saveSetting('service.general', general)} disabled={working === 'service.general'}><Save/>저장</Button>}/><div className="form-grid"><Field label="서비스 이름"><input value={general.serviceName} onChange={(event) => setGeneral({ ...general, serviceName: event.target.value })}/></Field><Field label="세션 유지 시간(분)"><input type="number" min="5" max="1440" value={general.sessionMinutes} onChange={(event) => setGeneral({ ...general, sessionMinutes: Number(event.target.value) })}/></Field><Field label="기본 시간대"><select value={general.defaultTimezone} onChange={(event) => setGeneral({ ...general, defaultTimezone: event.target.value })}><option value="Asia/Seoul">Asia/Seoul</option><option value="UTC">UTC</option></select></Field></div><SwitchField label="사용자 가입 허용" description="기본값은 꺼짐입니다. 끄면 관리자 생성 또는 OIDC 자동 등록만 허용합니다." checked={general.allowRegistration} onChange={(checked) => setGeneral({ ...general, allowRegistration: checked })}/></Card><Card><SectionHeader title="API 및 MCP" description="개인 키를 사용하는 외부 연동 정책입니다." action={<Button variant="primary" onClick={() => void saveSetting('api.access', api)} disabled={working === 'api.access'}><Save/>저장</Button>}/><SwitchField label="개인 API 키 인증" description="사용자가 발급한 최소 권한 키로 REST API에 접근합니다." checked={api.enabled} onChange={(checked) => setAPI({ ...api, enabled: checked, mcpEnabled: checked ? api.mcpEnabled : false })}/><SwitchField label="Streamable HTTP MCP" description="개인 키 권한 체계로 /mcp 도구를 제공합니다." checked={api.mcpEnabled} disabled={!api.enabled} onChange={(checked) => setAPI({ ...api, mcpEnabled: checked })}/><Field label="키별 분당 요청 한도"><input type="number" min="1" max="10000" value={api.rateLimitPerMinute} onChange={(event) => setAPI({ ...api, rateLimitPerMinute: Number(event.target.value) })}/></Field></Card><Card><SectionHeader title="이미지 업로드" description="JPEG, PNG, GIF, WebP 이미지의 용량과 게시물당 개수를 제한합니다." action={<Button variant="primary" onClick={() => void saveSetting('media.config', media)} disabled={working === 'media.config'}><Save/>저장</Button>}/><div className="form-grid"><Field label="파일당 최대 용량(MiB)" help="1~50MiB"><input type="number" min="1" max="50" value={Math.round(media.maxUploadBytes / (1024 * 1024))} onChange={(event) => setMedia({ ...media, maxUploadBytes: Number(event.target.value) * 1024 * 1024 })}/></Field><Field label="모인당 최대 이미지" help="1~12개"><input type="number" min="1" max="12" value={media.maxPerPost} onChange={(event) => setMedia({ ...media, maxPerPost: Number(event.target.value) })}/></Field></div></Card><Card><SectionHeader title="검토·승인 프로세스" description="설정이 없거나 꺼져 있으면 승인·반려 단계를 완전히 제외합니다." action={<Button variant="primary" onClick={() => void saveWorkflow()} disabled={working === 'workflow'}><Save/>정책 저장</Button>}/><SwitchField label="팀장 검토·승인 사용" description="지정 작업을 승인 대기로 보내고 승인 메뉴를 표시합니다." checked={workflow.enabled === true} onChange={(checked) => setWorkflow({ ...workflow, enabled: checked })}/>{workflow.enabled && <div className="nested-settings"><Field label="승인 대상 작업" help="쉼표로 구분합니다. 기본 게시 승인 코드는 post.publish입니다."><input value={(workflow.actions || []).join(', ')} onChange={(event) => setWorkflow({ ...workflow, actions: event.target.value.split(',').map((item) => item.trim()).filter(Boolean) })}/></Field><fieldset className="permission-picker"><legend>승인 가능한 역할</legend>{roleNames.map((role) => <label key={role}><input type="checkbox" checked={(workflow.approverRoles || []).includes(role)} onChange={(event) => setWorkflow({ ...workflow, approverRoles: event.target.checked ? [...(workflow.approverRoles || []), role] : (workflow.approverRoles || []).filter((item) => item !== role) })}/><span>{role}</span></label>)}</fieldset>{roleNames.length === 0 && <p className="field-error">approvals:review 권한이 있는 역할을 먼저 만드세요.</p>}</div>}</Card></div>}</div>;
}

interface AuditRow { id: string; actorUsername?: string; action?: string; target?: string; success?: boolean; ip?: string; createdAt?: string; detail?: unknown }
export function AdminAuditPage() {
  const [search, setSearch] = useState(''); const query = useApiQuery<unknown>(`/admin/audit?limit=100${search ? `&q=${encodeURIComponent(search)}` : ''}`); const rows = listFrom<AuditRow>(query.data as AuditRow[] | { items?: AuditRow[] } | undefined);
  return <div className="page-stack"><AdminTitle title="감사 로그" description="로그인, 관리 설정, 권한과 콘텐츠 조치 이력을 변경 불가능한 기록으로 추적합니다."/><Card><div className="table-toolbar"><label className="table-search"><Search/><input value={search} onChange={(event) => setSearch(event.target.value)} aria-label="감사 로그 검색" placeholder="수행자, 작업, 대상 검색"/></label><Button onClick={query.reload}><RefreshCw/>새로고침</Button></div>{query.loading ? <LoadingState/> : query.error ? <ErrorState message={query.error} onRetry={query.reload}/> : rows.length ? <Table caption="감사 로그" headers={['시각', '수행자', '작업', '대상', '결과', 'IP', '상세']}>{rows.map((row) => <tr key={row.id}><td>{formatDate(row.createdAt)}</td><td>@{row.actorUsername || 'system'}</td><td><strong>{row.action || '활동'}</strong></td><td>{row.target || '—'}</td><td><Badge tone={row.success === false ? 'danger' : 'positive'}>{row.success === false ? '실패' : '성공'}</Badge></td><td><code>{row.ip || '—'}</code></td><td>{row.detail !== undefined && <details><summary>보기</summary><pre>{JSON.stringify(row.detail, null, 2)}</pre></details>}</td></tr>)}</Table> : <EmptyState title="기록된 감사 이벤트가 없습니다" description="관리 활동이 시작되면 여기에 기록됩니다."/>}</Card></div>;
}
